
import React from 'react';
import { expect } from 'chai';
import { mount, shallow } from 'enzyme';
import Home from '../public/react/components/home';
import ReactTestUtils from 'react-addons-test-utils';

describe('<Home />', function () {

     it('should render </Home> DOM components', () => {
  
     });

     it('should show map when user perform search a website location', () => {
         
     });

     it('should show map when user perform a self position search location', () => {
         
     }); 

     it('should show table information data when user perfomrs any search ', () => {
         
     });

     it('should show information messages when user clicks on information button', () => {
         
     }); 

     it('should validate URL input format', () => {
         
     });
     
});